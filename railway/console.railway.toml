[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.console"

[deploy]
startCommand = "/app/scripts/start_console.sh"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

